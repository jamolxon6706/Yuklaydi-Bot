from __future__ import annotations

# All ReplyKeyboardMarkup menus removed — bot is now intent-driven.
# This stub exists only to avoid import errors during the transition.


def get_menu_action(text: str) -> None:
    return None
